apiVersion: v1
kind: Service
metadata:
  name: fitness-planner
  labels:
    app: fitness-planner
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: fitness-planner
